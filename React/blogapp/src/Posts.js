import React, { Component } from "react"; import Post from "./Post"; class Posts extends Component { constructor(props) { super(props); this.state = { posts: [], error: null }; } componentDidMount() { this.loadPosts(); } loadPosts() { fetch("https://jsonplaceholder.typicode.com/posts").then(res => res.json()).then(data => this.setState({ posts: data })).catch(err => this.setState({ error: err })); } componentDidCatch(error, info) { alert("Error occurred: " + error); } render() { if (this.state.error) return <div>Error</div>; return ( <div> {this.state.posts.map(post => <Post key={post.id} title={post.title} body={post.body} />)} </div> ); } } export default Posts;