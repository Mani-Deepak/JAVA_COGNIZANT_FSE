import React, { useState } from "react"; const CurrencyConvertor = () => { const [rs, setRs] = useState(0); const [euro, setEuro] = useState(0); const handleSubmit = (e) => { e.preventDefault(); setEuro(rs / 80); }; return ( <form onSubmit={handleSubmit}> <input type="number" value={rs} onChange={e => setRs(e.target.value)} /> <button type="submit">Convert</button> <div>Euro: {euro}</div> </form> ); }; export default CurrencyConvertor;