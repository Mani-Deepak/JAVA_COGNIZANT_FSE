import React, { useState } from "react"; const BookDetails = () => <div>Book Details Component</div>; const BlogDetails = () => <div>Blog Details Component</div>; const CourseDetails = () => <div>Course Details Component</div>; function App() { const [view, setView] = useState('book'); return ( <div> <button onClick={() => setView('book')}>Book</button> <button onClick={() => setView('blog')}>Blog</button> <button onClick={() => setView('course')}>Course</button> {view === 'book' && <BookDetails />} {view === 'blog' && <BlogDetails />} {view === 'course' && <CourseDetails />} </div> ); } export default App;